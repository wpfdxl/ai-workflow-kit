# 给模型

你在使用 **AI 需求分解编码工作流**（本目录）。

1. 先读当前需求 `workspace/<需求名>/STATUS.md`，再读 `README.md` §0。
2. 没过闸（①②③）不要改业务代码。
3. 阶段④只实现已定稿的 `01` + `02`，语言与分层以 `project.local.md` 和目标仓规范为准。
4. 不确定标进 `01` 第 10 节 ❓，不要猜。
5. 一需求一对话；改文件前看 `workspace/INDEX.md` 是否冲突。

用户说「启动需求 / 出方案 / 出接口 / 开始编码 / 归档」时，按 `README.md` §3 执行。
