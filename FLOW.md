# 流程展开

> `README.md`：怎么对模型说话。  
> 本文件：阶段怎么流、产出什么、文件怎么搬、多需求怎么并行。

---

## 1. 为什么

AI 编码达成率低，根因是上下文模糊且不可控。解法是分层写成文件，下一层只消费上一层已背书的内容。闸门：上一层没过，不准开下一层。

---

## 2. 三个工作目录

| 目录 | 放什么 | 何时动 |
|---|---|---|
| `workspace/` | 进行中的需求档案 | 启动到编码完成 |
| `docs/` | 对外接口文件（格式由本工程自定） | 阶段③；归档时整夹搬走 |
| `archive/` | 完工后的 00/01/02/STATUS/schema | 上线稳定后从 workspace 搬入 |

---

## 3. 四阶段

```
① 分解方案          模型写 01
        │
        ▼
② 人 review 方案     checklist 全过（人闸门）
        │
        ▼
③ 生成接口文档       模型写 02 ± docs/<需求>/
        │
        ▼
   人 review 接口
        │
        ▼
④ 编码               模型按 03 改业务代码
        │
        ▼
   自检 + 对照默认分支 review
        │
        ▼
   上线稳定 → 归档
```

对模型的具体说法见 `README.md` §3。

---

## 4. 标准路径

1. 用 `templates/` 在 `workspace/<需求名>/` 建 STATUS + 00。
2. 人填 `00`，模糊标 ❓。`INDEX.md`「在跑」加一行。
3. 模型读 `step1` + `00` 写 `01`。
4. 人用开发方案 checklist 过闸。
5. 模型读 `step2` 写 `02`；有对外 API 再写 `docs/<需求名>/`。
6. 把 01 第 9 节抄进 `03`。用户明确说可以编码后，模型读 `step3`，按第 9 节顺序实现。

口头补充：先写入 `00` 或 `01` 第 10 节，等人确认。禁止一句口头话覆盖已定稿 `02`。

---

## 5. INDEX 与冲突

启动需求 B 前，拿 B 要改的文件对在跑各行做交集。有交集 → git 分支隔离，或文档并行、编码排队。

「涉及代码文件」只列 **本次会改** 的。

---

## 6. 归档

编码完成、上线稳定后再搬。

```bash
ROOT=ai-dev-workflow   # 若拷到其他路径，改成实际目录名
mkdir -p "$ROOT/archive" "$ROOT/docs/archive"
mv "$ROOT/workspace/<需求名>" "$ROOT/archive/<需求名>"
if [ -d "$ROOT/docs/<需求名>" ]; then
  mv "$ROOT/docs/<需求名>" "$ROOT/docs/archive/<需求名>"
fi
```

然后改 `workspace/INDEX.md`：从「在跑」挪到「已归档」。

---

## 7. 多需求并行

| 策略 | 做法 | 适合 |
|---|---|---|
| 分支隔离（推荐） | 每需求一条 git 分支 | 可能改到相关联文件 |
| 串行编码 | 文档并行，编码排队 | 需求不多 |
| 文件清单比对 | 用 INDEX / 03 对交集 | 轻量兜底 |

1. 一需求一对话  
2. 需求名 = 文件夹名  
3. 编码前先看 INDEX  

---

## 8. 完成标志

| 阶段 | 可以进入下一阶段 |
|---|---|
| ①→② | `01` 已按模板写完，❓ 都在第 10 节 |
| ②→③ | 开发方案 checklist 全过，用户说方案定稿 |
| ③→④ | 接口 checklist 全过（或确认无对外 API）；`03` 已从第 9 节抄好 |
| ④→归档 | Task 全勾、自检过、对照默认分支无阻塞问题、上线稳定 |
| 归档完成 | workspace 已进 `archive/`，有接口文件则进 `docs/archive/`，INDEX 已搬家 |
