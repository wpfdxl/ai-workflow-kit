# 本工程绑定（每个仓库自己填）

> 工作流模板是通用的。**语言、路径、规范只写在这里**，不要写进 `templates/` / `prompts/`。

- 工程名：
- 主要语言 / 运行时：
- 业务代码根目录：（例 `src/`、`app/`）
- 默认对比分支：（例 `main` / `master` / `develop`）
- 规范文件：（例 `CONTRIBUTING.md`、`AGENTS.md`、`.cursor/rules/`）
- 接口文档格式：（例 OpenAPI 3、Markdown 表格、内部 JSON）
- 分层习惯：（例 handler → service → repository；或入口 → 业务 → 存储）
- 阶段④编码默认顺序：（默认可写：存储/schema → 业务逻辑 → 接口/入口 → 测试）
- Review 方式：（例 PR 模板、对照默认分支的 diff、仓库内 review skill 名称）
