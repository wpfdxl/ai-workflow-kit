# <需求名> 接口文档

> 阶段：③待人 review  
> 依赖：@01_开发方案.md  
> 同步：定稿后按 `project.local.md` 把对外文件写到 `docs/<需求名>/`（OpenAPI、Proto、Markdown、内部 JSON 等）。无对外 API 则本节写「无独立接口」。

## 1. 文档说明

- 模块：
- 数据来源：
- 适用端：
- 鉴权：
- 返回约定：顶层包装跟本工程现有接口一致，本文只详写业务字段

## 2. 接口清单

- `METHOD /path`：说明

## 3. <接口名> <方法路径>

### 3.1 请求

端 A：

`METHOD /path`

端 B（如有不同路径）：

`METHOD /path`

参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| | | | |

### 3.2 业务规则

- 
- 空态 / 无数据：
- 特殊展示：

### 3.3 返回字段

| 字段 | 类型 | 说明 |
|---|---|---|
| | | |

### 3.4 返回示例

#### 正常

```json
{}
```

#### 空态

```json
{}
```

#### 错误（如有业务错误码）

```json
{}
```

## 4. <接口名> <方法路径>

<!-- 复制上面 3.x -->

## 5. 联调建议

- 
