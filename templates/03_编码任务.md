# <需求名> 编码任务

> 阶段：④编码  
> 作业本。`@` 引用必须是人已过闸的确定信息。  
> 动手前读「上下文」，按「编码顺序」逐 Task，做完勾选，全部完成后跑自检。

## 上下文（编码前必读）

- 开发方案：@01_开发方案.md
- 接口文档：@02_接口文档.md
- 本工程绑定：@../../project.local.md
- 目标仓规范：（把 `project.local.md` 里列出的文件 @ 过来）
- 相关 skill / 文档（如有）：@

## 编码顺序

> 默认：存储 → 业务 → 缓存 → 入口 → 测试。以方案第 9 节和 `project.local.md` 为准。

1. 每个 Task 先搜索仓库里的相似实现，再写。
2. 入口层不直连存储；跨模块走对方业务层。
3. 命名、分层、注释风格跟目标仓现有代码对齐。
4. 最小修改：不格式化无关文件，不重构方案没写的部分。
5. 实现必须与定稿 01 / 02 的字段、路径、状态机一致。
6. 每完成一个 Task：勾选，并简述改了哪些文件。

## Task 清单（从方案第 9 节复制）

- [ ] Task1：
  - 涉及文件：
  - 自测：
- [ ] Task2：
  - 涉及文件：
  - 自测：
- [ ] TaskN：对照 `checklists/编码完成自检.md` + 默认分支 review

## 完成标志

- [ ] 全部 Task 勾选
- [ ] 编码自检全过
- [ ] 对照 `project.local.md` 的默认分支 review，无阻塞级问题
